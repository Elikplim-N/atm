# Only needed when the client and server are deployed as separate projects
# (e.g. two Vercel projects). Leave unset for local dev — Vite's dev server
# proxies /api to the local server instead (see vite.config.js).
VITE_API_URL=https://your-server-project.vercel.app
